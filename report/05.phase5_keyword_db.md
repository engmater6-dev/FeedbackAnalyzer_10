# Phase 5 SQLite 감정 키워드 DB (R-10)

| 항목 | 내용 |
|------|------|
| 브랜치 | `new_feature` |
| 문서 | [doc/KEYWORD_DB.md](../doc/KEYWORD_DB.md) |
| 일자 | 2026-05-22 |

## 구현 요약

- `keyword_db` — SQLite 시드·CRUD·`get_sentiment_keyword_map()`
- `sentiment.py` — DB + 캐시 SSOT (`invalidate_sentiment_cache`)
- `handlers/keywords.py` — `POST /settings/keywords`
- `HtmlRenderer` — 키워드 목록·추가·삭제 UI

## 데모

1. `python app.py` → 대시보드 **감정 키워드 관리** 확인
2. 긍정 키워드 추가 → `/analyze`로 해당 문장 입력 → 긍정 집계
3. 키워드 삭제 → 분류 변경 확인

## 연동

- `analysis_service.annotate_feedbacks` → `classify_sentiment`
- `filter_service` → 동일 `classify_sentiment`
- Golden Master — 시드=기존 constants, baseline 유지
