# 00.spec — 요청 프롬프트·축약 답변

> 본 세션에서 사용자가 요청한 프롬프트 **원문**과 **축약 답변** 기록.

---

## SP-00 | 문제 정의·PRD·TDD To-Do (분석만)

### 프롬프트 (원문)

```
[P] 당신은 문제 정의 전문가입니다.
[T] @project_purpose.md , @README.md @src 내용을 분석하여
     현 프로젝트의 문제점, PRD, TO-DO List를 TDD 단계별로 정리합니다.
[F] 파일 및 코드 변경은 금지하고, 정리 내용을 PRD, README에 넣을 수 있는 내용으로 정리하여 보여주기만 합니다.
```

### 답변 (축약)

- **문제점**: 감정/필터 규칙 3중 정의, God Function, 전역 상태, 테스트 없음, CSV·다운로드 불일치 등.
- **PRD 초안**: 목적·As-Is 기능·버그 B-01~B-06·Phase To-Be·TDD 단계표 제시 (파일 미생성).
- **TDD To-Do**: Red(테스트)→Green(버그 수정)→Refactor(구조) 6단계 체크리스트 제시.

---

## SP-01 | 전체 구조·미션 안내 분석

### 프롬프트 (원문)

```
@c:\DEV\FeedbackAnalyzer_10\  전체구조·미션 안내 분석해줘
```

### 답변 (축약)

- 저장소: `doc/`, `report/`, `sample/`, `src/python/` 플랫 8모듈.
- 미션: `project_purpose.md` §6.1 — 8단계·약 13시간 (테스트 90% → 버그 → 리팩토링 → Trend·DB → 리뷰).
- 런타임: Flask `app.py` 중심, Session + fil_data + analyzer/filter 이중 규칙.

---

## SP-02 | PRD·README·.cursorrules 생성

### 프롬프트 (원문)

```
분석한 내용을 기반으로 doc폴더 하부에 PRD.md 파일을 생성하고, README 파일에 To Do List를 포함하여 새로운 내용을 업데이트해줘.
.cursorrules 파일도 위 내용을 기반으로 생성해줘
```

### 답변 (축약)

- `doc/PRD.md` 생성 (요구사항·버그·TDD·API).
- `README.md` To Do List·구조·알려진 이슈 반영.
- `.cursorrules` — TDD·스멜·버그·미션 Phase 가이드.

---

## SP-03 | 브랜치 B_10 생성

### 프롬프트 (원문)

```
B_10 branch 생성해줘
```

### 답변 (축약)

- `main` 기준 `B_10` 생성·체크아웃 완료 (`ac19aa4`).

---

## SP-04 | spec 브랜치 생성

### 프롬프트 (원문)

```
B_10 branch를 base로 spec branch를 생성해줘
```

### 답변 (축약)

- `B_10`에서 `spec` 브랜치 생성·체크아웃. 동일 커밋 `ac19aa4`.

---

## SP-05 | Mom Test

### 프롬프트 (원문)

```
@c:\DEV\FeedbackAnalyzer_10\ Mom Test 진행 해줘
```

### 답변 (축약)

- 문서 가설 vs 런타임 검증. PRD 예시 문장 부정 분류 실패, 중립 필터 불일치.
- 교육용 ✅ / 실무 VOC 도구 ❌ (현재).
- `doc/MOM_TEST.md` 작성. (이전 `report/` → `doc/` 이동)

---

## SP-06 | 코드 스멜 분석

### 프롬프트 (원문)

```
@doc/MOM_TEST.md @doc/PRD.md @README.md 를 참조해서 @src/ 코드 스멜 분석해줘
```

### 답변 (축약)

- 8모듈 스멜 ID 매핑 (S-A01~S-FH02). 핵심: `S_KEYWORDS` vs `constants` vs analyzer 불일치 → B-01/B-02.
- God Function, 전역 상태, Lava Flow, Anemic Model 등. Phase별 제거 우선순위 제시 (파일 미생성).

---

## SP-07 | CODE_SMELL 보고서·README To-Do 갱신

### 프롬프트 (원문)

```
진행하고, 분석한 결과를 바탕으로 README 파일에 To Do list 포함하여 업데이트해줘
```

### 답변 (축약)

- `doc/CODE_SMELL.md` 생성. (이전 `report/` → `doc/` 이동)
- `README.md` To Do — 스멜 ID·Mom Test·PRD 버그 매핑, Phase 0~6 상세 체크리스트.

---

## SP-08 | spec·report 아카이브 (본 요청)

### 프롬프트 (원문)

```
현재 내가 요청한 프롬프트는 원문 그대로, 답변은 축약해서 prompt 폴더안에
00.spec.md로 만들어주고, 
결과는 report 폴더안에 00.sp_00.md로 보고서 만들어줘
```

### 답변 (축약)

- 본 파일(`prompt/00.spec.md`) 및 `report/00.sp_00.md` 생성.

---

## 산출물 인덱스

| 파일 | 설명 |
|------|------|
| `doc/PRD.md` | 제품 요구사항 |
| `doc/MOM_TEST.md` | Mom Test 보고서 |
| `doc/CODE_SMELL.md` | 코드 스멜 분석 |
| `doc/test_plan.md` | 테스트 계획서 |
| `report/00.sp_00.md` | 본 세션 통합 결과 보고서 |
| `.cursorrules` | Cursor 작업 규칙 |
| `README.md` | 설치·To Do (스멜 연계) |
