# 02.green — 요청 프롬프트·축약 답변

> TDD **Green** 단계 세션. 사용자 프롬프트 **원문** + **축약 답변**.  
> 결과 보고: [report/02.green.md](../report/02.green.md)

---

## GD-01 | 브랜치 `green` 생성

### 프롬프트 (원문)

```
red branch 만들어줘
```

(실제 요청: `green branch 만들어줘`)

### 답변 (축약)

- `red` 기준 `green` 브랜치 생성·체크아웃 (`39c4ed5`).
- 원격: `git push -u origin green` 별도.

---

## GD-02 | 진행 단계 분석 (doc·README·src)

### 프롬프트 (원문)

```
@doc/ @README.md @src/ 를 분석하여 현 진행 단계 알려줘
```

### 답변 (축약)

- Phase 0 대부분 완료, **Phase 1 Red 완료** (4 fail / 2 pass).
- 다음: **Phase 2 Green** — B-01/B-02 우선, 브랜치 `green`.

---

## GD-03 | Green Step별 할 일

### 프롬프트 (원문)

```
Green 단계에서 할 일을 step별로 나누어서 알려줘
```

### 답변 (축약)

- Step 0~3: Red 확인 → B-01 → B-02 → Gate 6 pass.
- Step 4~5: UX·B-03~B-06.
- Step 6: cov 90%+, IT.
- 이후 Phase 3 Refactor.

---

## GD-04 | Step 0 + 커밋

### 프롬프트 (원문)

```
step 0 진행하고 커밋커멘트를 "Red 기준선 재확인"으로 작성해줘
```

### 답변 (축약)

- pytest 6건 **4 fail / 2 pass** 재확인.
- `report/green_step0_baseline.md` · 커밋 `518915f`.

---

## GD-05 | Step 1 + 커밋

### 프롬프트 (원문)

```
step 1 진행하고 커밋커멘트를 "감정 규칙 단일화"로 작성해줘
```

### 답변 (축약)

- `S_KEYWORDS` 제거, `classify_sentiment()` SSOT, `화가` 추가.
- 5 passed / 1 failed · 커밋 `8dff0f2`.

---

## GD-06 | Step 2 + 커밋

### 프롬프트 (원문)

```
step 2 진행하고 커밋커멘트를 "카테고리 main 규칙 통일"로 작성해줘
```

### 답변 (축약)

- `matches_category()` — `kw`·filter `main` 통일.
- **6 passed** · 커밋 `5bd0d1c`.

---

## GD-07 | Step 3 + 커밋

### 프롬프트 (원문)

```
step 3 진행하고 커밋커멘트를 "Gate 확인"으로 작성해줘
```

### 답변 (축약)

- Domain Green Gate 6 passed 재검증.
- `report/green_step3_gate.md` · 커밋 `845d151`.

---

## GD-08 | Step 4 + 커밋

### 프롬프트 (원문)

```
Step 4 동일하게 진행
```

### 답변 (축약)

- `filters` print 제거, textarea 멀티라인·줄 단위 `/analyze`.
- Gate 유지 · 커밋 `cfaefea` (`부수 정리`).

---

## GD-09 | Step 5 + 커밋

### 프롬프트 (원문)

```
동일하게 진행
```

(Step 5 — B-03~B-06)

### 답변 (축약)

- `fil_data`→Session, CSV `text`, upload 분석, Logger UI.
- 커밋 `c6b9ff4` (`B-03~B-06 적용`).

---

## GD-10 | README 진행사항 갱신

### 프롬프트 (원문)

```
현재까지 진행사항 README 파일에 업데이트 해줘
```

### 답변 (축약)

- 브랜치 `green`, Phase 1~2 완료, GREEN To-Do Step 0~6 반영.
- (로컬 갱신, 별도 커밋 가능)

---

## GD-11 | QA 분석

### 프롬프트 (원문)

```
@doc/ @README.md @src/ 를 QA 분석해줘
```

### 답변 (축약)

- Domain Green ✅, DoD(cov·IT) ❌ → Step 6 권장.
- 잔여: Logger 누적, doc 구버전, God Function 등.
- 이후 `doc/defect_list.md` 22건 등록.

---

## GD-12 | 미완료 결함 일정

### 프롬프트 (원문)

```
미완료된 결함은 언제 개선할 예정이지?
```

### 답변 (축약)

- Step 6 → Phase 3 → Phase 6 순.
- Gate 막는 일 없음; Refactor·문서·발표 단계에 매핑.

---

## GD-13 | Step 6 + 커밋

### 프롬프트 (원문)

```
진행
```

(Step 6 — IT·cov·UX)

### 답변 (축약)

- IT 8건+, cov **97.42%**, DEF-013/014 보완.
- **39 passed** · 커밋 `b52f5d7` · `report/green_step6_tests.md`.

---

## GD-14 | defect_list Phase 체크리스트

### 프롬프트 (원문)

```
@doc/defect_list.md 작업 순서 체크리스트를 phase 6에 맞추어 더 쪼개서 정리해줘
```

### 답변 (축약)

- §6 Phase 0~6 세분화, Phase 6 Gate(리뷰·문서·발표·QA 종결).
- DEF→Phase 매핑 표 추가.

---

## GD-15 | 단계 정리

### 프롬프트 (원문)

```
현재까지 단계 정리해줘
```

### 답변 (축약)

- Green Step 0~7( Golden Master 포함) 완료 요약.
- Phase 3~6·QA 미완 5건 잔여.

---

## GD-16 | 수동 E2E 설명

### 프롬프트 (원문)

```
수동 E2E가 뭐야?
```

### 답변 (축약)

- 서버 기동 후 브라우저로 실제 UI 흐름 확인 (자동 pytest와 별도).
- Phase 0·Phase 6 증거용; README `[ ]` 항목.

---

## GD-17 | Golden Master (Approval)

### 프롬프트 (원문)

```
[P] 회귀 테스트(Approval/Golden Master) 설계 전문가입니다.
[C] Python 3.11+, pytest, pydantic
[T] @c:\DEV\FeedbackAnalyzer_10\ 출력 기반 Golden Master 기준 파일을 생성해줘.

    1) 기준 출력(tests/golden_master_expected.txt) 생성/보관 전략
       생성 후: git add tests/golden_master_expected.txt  (버전 관리 필수)

    2) approve 패턴 적용:
       - 기준 파일 없으면 → 현재 출력을 기준으로 자동 생성
       - 기준 파일 있으면 → actual vs expected 문자열 비교
       - 불일치 시: diff 출력 후 테스트 FAIL

[F] tests/golden_master_expected.txt 파일 내용 + 생성 스크립트
```

### 답변 (축약)

- `golden_master_expected.txt`, `golden_master_capture.py`, `approve_golden_master()`.
- `scripts/generate_golden_master.py` (`--force`, `--check`).
- `tests/GOLDEN_MASTER.md`, `test_golden_master.py` — 39 tests에 포함.

---

## GD-18 | defect_list·README 갱신

### 프롬프트 (원문)

```
현재까지 진행사항을 분석해서 추가 보완 진행이 필요한 사항 알려줘
```

(이후 README·defect_list 업데이트 요청)

### 답변 (축약)

- Step 6·Golden·문서 동기화 권장 목록 정리.
- `README.md`, `doc/defect_list.md` 갱신.

---

## GD-19 | 아카이브 (본 요청)

### 프롬프트 (원문)

```
현재 내가 요청한 프롬프트는 원문 그대로, 답변은 축약해서 prompt 폴더안에
02.green.md로 만들어주고, 
결과는 report 폴더안에 02.green.md로 보고서 만들어줘
```

### 답변 (축약)

- `prompt/02.green.md`, `report/02.green.md` 생성.

---

## 참고

- 선행: [01.red.md](01.red.md) · [report/01.red.md](../report/01.red.md)
- 브랜치: `green`
- 커밋: `518915f` → `8dff0f2` → `5bd0d1c` → `845d151` → `cfaefea` → `c6b9ff4` → `b52f5d7`
