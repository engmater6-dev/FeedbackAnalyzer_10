[pytest]
testpaths = tests
pythonpath = .
addopts = -ra --strict-markers

markers =
    p0: anchor / release gate
    red: expected fail until phase 2 green
    domain: domain layer only
    boundary: flask / io
